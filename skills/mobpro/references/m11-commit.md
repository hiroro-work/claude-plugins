# mobpro M11 Commit

The inputs that `SKILL.md` **M11 — Commit (USER GATES)** depends on live here, and the Procedure that step delegates to consumes them as it runs. That step keeps its entry condition, its `../dev-workflow/references/interactive-commits.md` delegation, and the "point of this diff" note inline. **Read this file once**, at M11. Unqualified `§ <Heading>` references resolve to `SKILL.md`.

**Boundary chain input**: M6 normally leaves `implementation_boundaries` populated, so `../dev-workflow/references/interactive-commits.md` § Propose commit plan takes its chain-present path — the commit plan a junior sees is **one commit per implementation unit they reviewed**, in the order they reviewed them, rather than regrouped from the finished tree, followed by that section's final group for whatever the quality gates changed afterwards. When a unit's object build failed, [`diff-review.md`](diff-review.md) § Per-unit review range emptied the list, and that section takes its chain-absent path instead. Contract in [`inline-defs.md`](inline-defs.md) § (e).

**Diff surface (`commit_review_gate` branch)**: `diff` — the default — presents the diff in chat. `crit` **probes first, then reads** (`Keep in sync with dev-workflow references/interactive-commits.md's Diff-review mode branch probe paragraph, which names the owning sections.`): run `crit --version` and `printenv CLAUDE_CODE_REMOTE` (both unconditionally, each a cheap sub-second call; never substitute `command -v crit`, which sits outside the `Bash(crit *)` grant), cache the pair as `crit_commit_review_available`, and only when both clear read `../dev-workflow/references/crit-commit-review.md` and follow it: the story prologue handed to crit before each launch, launch, outcome mapping. When either probe fails, skip that read entirely, present the diff in chat as the `diff` branch does, and emit **one** informational note for the whole of M11 — not per commit — in the resolved `language`: `crit --version` failed → name the install URL (<https://github.com/tomasz-tomczyk/crit>); only the reachability probe failed → name that no local browser is reachable; both failed → name the unavailability. M6 emits its own such note once for its loop (`diff-review.md` § crit path). Probing before reading is what keeps an unavailable crit from costing a full read of that file, and it mirrors `m5-plan-approval.md` sub-step 1's **Browser-reachability probe** bullet. It launches crit via **background Bash**, not the `Agent` tool (§ Direct Agent dispatch sites). On the `crit` path, emit the "point of this diff" note **in the same turn as the crit launch, before the junior opens the browser**, so it guides their reading. The same key governs M6's per-unit diff review ([`diff-review.md`](diff-review.md)); each gate probes crit availability and emits its own unavailability note independently. That reference's § Round re-verification weight maps onto phases this workflow already has: on the **actual-code** branch the Step 7 (Check / Test) + Step 7.5 (Rules Compliance Review) re-runs map to M8 and M9 sub-step 2 (rules compliance) only — not M9's code review; on the **metadata-only** branch only the `check_commands` half of M8 re-runs, and M9 is not re-entered at all. Both are verification passes here, so § Learning-Stop Principle's "Primary-pass rule" paragraph applies: no prediction narration. M9 sub-step 5's aggregate has already run by the time M11 (Commit) executes, so a round's rules fixes get no later cycle: an actual-code round's M9 sub-step 2 re-run therefore **continues into** `../dev-workflow/references/step7.5-rules-compliance.md`'s sub-steps (b)–(d) rather than stopping after (a).

**Step 10 inline definitions**: `interactive-commits.md` depends on definitions that live inline in dev-workflow's SKILL.md — `<base-commit>` (whose upstream is dev-workflow's § Step 2, not § Step 10) and the phase names the Procedure cites, both mapped in that reference, the Approval token closed list (which `crit-commit-review.md`'s comment classifier also points at, so the `crit` path resolves it here too), Localized summary tokens, the `landed_count` lifecycle, the Post-hook attribution check, and the branch-ancestry guard. Read [`inline-defs.md`](inline-defs.md) § (e) and apply them.
