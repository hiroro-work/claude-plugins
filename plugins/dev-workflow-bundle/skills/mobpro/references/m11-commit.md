# mobpro M11 Commit

The inputs that `SKILL.md` **M11 — Commit (USER GATES)** depends on live here. **Read this file once**, at M11. Unqualified `§ <Heading>` references resolve to `SKILL.md`.

**Boundary chain input**: contract in [`inline-defs.md`](inline-defs.md) § (e)'s **Build-order boundary chain** bullet.

**Diff surface (`commit_review_gate` branch)**: `diff` — the default — presents the diff in chat. `crit` **probes first, then reads** (`Keep in sync with dev-workflow references/interactive-commits.md's Diff-review mode branch probe paragraph, which names the owning sections.`): run `crit --version` and `printenv CLAUDE_CODE_REMOTE` **once at M11 entry, before the Procedure below begins** (both unconditionally; never substitute `command -v crit`, which sits outside the `Bash(crit *)` grant) — cache the pair as `crit_commit_review_available`, and — only when both clear, and only once the first commit that actually reaches crit gets there, not at entry with the probes — read `../dev-workflow/references/crit-commit-review.md` and follow it. When either probe fails, skip that read entirely, present the diff in chat as the `diff` branch does, and emit **one** informational note for the whole of M11 — not per commit — in the resolved `language`: `crit --version` failed → name the install URL (<https://github.com/tomasz-tomczyk/crit>); only the reachability probe failed → name that no local browser is reachable; both failed → name the unavailability. It launches crit via **background Bash**, not the `Agent` tool (§ Direct Agent dispatch sites). On the `crit` path, emit the "point of this diff" note **in the same turn as the crit launch, before the junior opens the browser**. The same key governs M6's per-unit diff review ([`diff-review.md`](diff-review.md)); each gate probes crit availability and emits its own unavailability note independently. That reference's § Round re-verification weight maps onto phases this workflow already has: on the **actual-code** branch the Step 7 (Check / Test) + Step 7.5 (Rules Compliance Review) re-runs map to M8 and M9 sub-step 2 (rules compliance) only — not M9's code review; on the **metadata-only** branch only the `check_commands` half of M8 re-runs, and M9 is not re-entered at all. § Learning-Stop Principle's "Primary-pass rule" paragraph applies: no prediction narration. An actual-code round's M9 sub-step 2 re-run **continues into** `../dev-workflow/references/step7.5-rules-compliance.md`'s sub-steps (b)–(d) rather than stopping after (a).

**Already-reviewed skip option (commit-plan gate)**: at the **commit-plan approval gate** — `../dev-workflow/references/interactive-commits.md` § Propose commit plan — offer the junior a way to skip the review of the commits whose content they already reviewed at M6's per-unit diff review — both the crit round and the per-commit accept gate, so the plan approval they are already giving is the only confirmation those commits need. Extend that gate from here: that file's § Per-commit loop carries the carve-out that admits the skip itself.

**Offer the option only when all three hold.** (1) `commit_review_gate` resolves to `crit`. (2) Both crit probes cleared — reuse the `crit_commit_review_available` the **Diff surface (`commit_review_gate` branch)** paragraph above cached at M11 entry, and run no probe of your own. (3) The plan being proposed carries at least one group whose `tree source` is a boundary object (`../dev-workflow/references/interactive-commits.md` § Propose commit plan's **Chain present** path). Whichever condition fails, present the gate exactly as `interactive-commits.md` writes it and reset `m11_crit_skip_reviewed` to `false`.

**What the option adds**: name which of the listed commits it would cover, then offer one further way to accept alongside the plain one — approve the plan **and** treat those commits as already reviewed, which drops both their crit round and their per-commit accept gate, so this plan approval is the only confirmation they get. Leave out of that coverage any group `../dev-workflow/references/interactive-commits.md` § Propose commit plan's **Plan-scope cross-reference** paragraph has already stripped a path from **and every group after it**. Judge the reply per [`inline-defs.md`](inline-defs.md) § (e)'s **Approval token closed list** bullet unchanged: this is a **variant of that list's `accept` bucket, not a fifth bucket**. A plain accept sets `m11_crit_skip_reviewed = false` and an accept carrying the skip request sets it `true`; only an accept writes the variable.

**`m11_crit_skip_reviewed`**: `false` at M11 entry; written by the offer-condition check above, which resets it to `false` whenever one of the three conditions fails, and by a reply to the commit-plan approval gate, so a `Mid-loop adjust` branch that rebuilds the plan and re-presents that gate overwrites it with that round's evaluation and reply; read once per commit at the application below. M11-local — **not** a `SKILL.md` § Cross-step state variables member.

**Where the skip applies**: at `../dev-workflow/references/interactive-commits.md` § Per-commit loop sub-step `a`'s **Diff-review mode branch**, skip crit for a commit when **both** hold — `m11_crit_skip_reviewed` is `true`, **and** this commit's group obtained its tree **from a boundary object** (that sub-step's **From a boundary object** derivation). Skipping means the commit takes neither review surface: no crit launch, no read of `../dev-workflow/references/crit-commit-review.md`, no chat rendering of the **Diff** element, and no sub-step `b` accept gate. Subject, Body, Files, and Verification still render as that presentation specifies, and the commit lands without waiting. The "point of this diff" note goes out as it always does — on a skipped commit it accompanies the landing notice. When either condition fails the commit goes to crit as usual — and when it fails for a commit the gate had named as covered, say so in one line in the resolved `language` before the launch.

**Step 10 inline definitions**: read [`inline-defs.md`](inline-defs.md) § (e) and apply the definitions `interactive-commits.md` depends on. Its **Approval token closed list** also resolves the pointer `crit-commit-review.md`'s comment classifier makes to `finish-phase.md` § Approval token closed list.
